设置实例的属性可以在实例创建后任意时间进行，也可以在能够访问实例的代码中进行。构造器__init__()是设置这些属性的关键点之一
能够在运行时创建实例属性，是Python类的优秀特性之一
属性在条件语句中创建，如果该条件语句并未被执行，属性也就是不存在
1、在构造器中首先设置实例属性
一旦__init__()执行完毕，返回实力对象，即完成了实例化过程
2、默认参数提供默认的实例安装
带默认参数的__init__()提供一个有效的方式初始化实例
3、__init__()方法应当返回（return）None
