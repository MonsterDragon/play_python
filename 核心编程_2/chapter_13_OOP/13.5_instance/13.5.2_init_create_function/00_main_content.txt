当类被调用，实例化的第一步是创建实例。一旦对象创建了，Python检查是否实现了__init__()方法。默认情况下，如果没有定义特殊方法__init__()，対实例不会施加任何特别的操作
如果__init__()已经被实现，那么它被调用，实力对象作为第一个参数（self）被传递进去，像标准方法调用一样。调用类时，传进的任何参数都交给__init__()
__init__()，是在解释器为你创建一个实例后调用的第一个方法
