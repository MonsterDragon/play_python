授权是包装的一个特性，可用于简化处理相关命令性功能，采用以存在的功能以达到最大先得的代码重用
授权的过程，即是所有更新的功能都是由新类的某部分来处理，但以存在的功能就授权给对象的默认属性
实现授权的关键点就是覆盖__getattr__()方法，在代码中包含一个对getattr()内建函数的调用。特别地，调用getattr()以得到默认对象（数据属性或者方法）并返回它以便访问或者调用。特殊方法__getattr__()的工作方式是，当搜索一个属性时，任何局部对象首先被找到。如果搜索失败了，则__getattr__()会被调用，然后调用getattr()得到一个对象的默认行为
1、
