1、访问模型分类：直接存取、顺序和映射
2、
直接存取：所有容器类型可以直接访问。多有的数值类型都归为这一类
顺序：序列类型是指容器内的元素按从0开始的索引顺序访问。一次可以访问一个元素或多个元素。字符串、列表、元组
映射：它的元素无序存放，通过唯一的键来访问，这就是映射类型，它容纳的是哈希键值对的集合

数据类型	存储类型	更新模型	访问模型

数字		原子		不可变		直接存取
字符串		原子		不可变		直接存取
列表		容器		可变		顺序存取
元组		容器		不可变		顺序存取
列表		容器		可变		映射存取

